#include <iostream>
using namespace std;

int main(){
	float final, contador;
	cout << "Ingrese el limite que desea: ";
	cin >> final;
		while (contador < final){
		contador = contador + 1;
		cout << "Contador: " <<contador << endl;	
		}
}
